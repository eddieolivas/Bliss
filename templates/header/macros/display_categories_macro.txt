<% registerMacro('displayCategoriesMenu', function (item) {
	if (!item.data)
	{
		item.data = {};
	}
	// we want all navbar links to preserve current url option 'display'
	//item.data['keep-options'] = 'display';
	%>
	<li class="dropdown-submenu">
			a
	</li>
<% }) %>